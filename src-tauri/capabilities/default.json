{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the Reminder window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
